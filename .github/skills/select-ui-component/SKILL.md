---
name: select-ui-component
description: Check if a reusable UI component exists in `projects/components/src/lib` before creating a new one. Use for buttons, form controls, modals, tables, etc. Triggers when starting to implement a new UI element or feature.
---

Verificar `projects/components/src/lib` antes de crear UI
